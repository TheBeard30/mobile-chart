import { SelectPickerDirective } from './select-picker.directive';

describe('SelectPickerDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectPickerDirective();
    expect(directive).toBeTruthy();
  });
});
