import { G2BarChartDirective } from './g2-bar-chart.directive';

describe('G2BarChartDirective', () => {
  it('should create an instance', () => {
    const directive = new G2BarChartDirective();
    expect(directive).toBeTruthy();
  });
});
